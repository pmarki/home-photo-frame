# Directory containing source photos
PHOTOS_DIR=./photos

# Directory for thumbnail cache
CACHE_DIR=./cache

# Directory for the SQLite database file (files.db)
# Defaults to the directory of the running binary
# DB_DIR=./

# Listen address (use :8080 or 0.0.0.0:8080)
PORT=:8080
